[project]
name = "minis-complete-agent"
version = "1.0.0"
description = "Evidence-gated mission orchestrator with skill, connector, subagent, and verification control"
requires-python = ">=3.11"
dependencies = ["requests"]

[project.scripts]
minis-complete = "orchestrator:main"

[tool.setuptools]
py-modules = ["orchestrator", "engine"]